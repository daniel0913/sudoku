#ifndef MAIN_H
#define MAIN_H

/*
 * Exits the program with status `status` but prints the usage
 * instructions beforehand. It also cleans up (like freeing the grid
 * pointed to by the global variable.
 */
void usage (int status);

#endif /* MAIN_H */
